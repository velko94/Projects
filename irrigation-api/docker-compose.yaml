version: '3.8'

services:
  irrigation-app:
    build: .
    container_name: my-irrigation-app
    ports:
    - "8001:8000"
    environment:
    - irrigation_api_key=poliva_samo_velko
    restart: always
  db:
    image: postgres:15-alpine
    container_name: irrigation-db
    environment:
      - POSTGRES_USER=velko_admin
      - POSTGRES_PASSWORD=strashna_parola_123
      - POSTGRES_DB=irrigation_system
    ports:
      - "5432:5432"
volumes:
  postgres_data: